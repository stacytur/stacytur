@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
}
@model kurs.Models.Dostavka

@*@{
    ViewData["Title"] = "Orders";
}

<h1>Orders for Delivery ID: @Model.IdDostavka</h1>

<table class="table">
    <thead>
        <tr>
            <th>Date</th>
            <th>Sum</th>
            <!-- Другие заголовки -->
        </tr>
    </thead>
    <tbody>
        @foreach (var zakaz in Model.Zakaz)
        {
            <tr>
                <td>@zakaz.ZakazData.ToString("dd.MM.yyyy")</td>
                <td>@zakaz.ZakazSumma</td>
                <!-- Другие ячейки -->
                <!-- Вывод информации о товарах для данного заказа -->
                <td>
                    <ul>
                        @foreach (var tovar in zakaz.Tovar)
                        {
                            <li>@tovar.NazvanieTovara</li>
                            <!-- Другие свойства товара -->
                        }
                    </ul>
                </td>
            </tr>
        }
    </tbody>
</table>*@


@{
    ViewData["Title"] = "Orders";
}

<h1>Orders for @Model.StatusDostavki</h1>

<table class="table">
    <thead>
        <tr>
            <th>Date</th>
            <th>Sum</th>
            <!-- Другие заголовки -->
        </tr>
    </thead>
    <tbody>
        @foreach (var zakaz in Model.Zakaz)
        {
            <tr>
                <td>@zakaz.ZakazData.ToString("dd.MM.yyyy")</td>
                <td>@zakaz.ZakazSumma</td>
                <!-- Другие ячейки -->
            </tr>
        }
    </tbody>
</table>